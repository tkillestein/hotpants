/*
 * HOTPANTS Global Variable Definitions
 *
 * This file defines all global variables used by the core algorithms.
 * It is compiled once into the shared library to avoid multiple definitions.
 *
 * All other source files (alard.c, functions.c) declare these as extern.
 */

#define HOTPANTS_DEFINE_GLOBALS
#include "globals.h"
